# AI Dev Agents

[🇪🇸 Leer en español](README.es.md)

Specialized agent system for AI-assisted development. Language, framework and AI tool agnostic. Supports Clean Architecture, MVC, MVVM and None (~90% of all software projects).

## What Is This

12 specialized agents + 1 orchestrator that guide AI assistants during software development. Includes a cross-verification pattern between agents to prevent errors that a single agent cannot detect.

**Key principle:** Agents define WHAT to verify (agnostic Quality Gates). Skills define HOW to do it (framework-specific).

<details>
<summary><strong>New here? Start here</strong></summary>

### Step 1: Try one agent

Pick a task you're already doing (e.g., writing a test) and invoke `@tdd-developer`. See if the gates catch something you'd miss. No setup beyond Quick Start needed.

### Step 2: Add your project context

Run the bootstrap prompt (`.ai/prompts/bootstrap.md`) in your AI tool. It generates `project-context.md`, `CLAUDE.md`, `decisions.md` and stack skills automatically. Agents go from generic to project-aware.

### Step 3: Combine agents

- **Tools with on-demand agents** (Claude Code, OpenCode, Antigravity, Gemini CLI, Codex CLI): invoke agents individually or use `@orchestrator` to route and coordinate cross-verification automatically.
- **Tools without on-demand agents** (Cursor, Windsurf, Copilot): agents are loaded inline — invoke them by name in your prompt.

### Step 4: Feature Specs (SDD)

Generate a spec with `.ai/prompts/feature-spec.md` before implementing. Each agent extracts what it needs from the spec. Recommended for all tools — nearly essential for Cursor/Windsurf/Copilot where it centralizes context that inline agents cannot load on demand. See [when to use specs](.ai/docs/persistent-memory.md).

> Full walkthrough: [getting-started guide](.ai/docs/getting-started.md) (5 minutes).

</details>

## Tool Compatibility

| Tool | Always-loaded file | On-demand agents | Token savings |
|---|---|---|---|
| Claude Code | `CLAUDE.md` | `.claude/agents/` (yes) | High |
| OpenCode | `AGENTS.md` | `.opencode/agents/` (yes) | High |
| Antigravity (Google) | `GEMINI.md` | `.agents/rules/` (yes, @mention) | High |
| Gemini CLI | `GEMINI.md` | `.gemini/skills/` (yes, activate_skill) | High |
| Codex CLI (OpenAI) | `AGENTS.md` | `.agents/skills/` (yes, /skills) | High |
| Continue (VS Code) | `.continue/rules/` | Partial (implicit matching) | Medium |
| Cursor | `.cursorrules` | No (all inline) | Low |
| Windsurf | `.windsurfrules` | No (all inline) | Low |
| GitHub Copilot / Copilot CLI | `.github/copilot-instructions.md`, `AGENTS.md` | CLI: yes (AGENTS.md) / IDE: no | CLI: High / IDE: Low |

Tools with on-demand agents/skills (Claude Code, OpenCode, Antigravity, Gemini CLI, Codex CLI) load agents ONLY when invoked — high token savings. Tools without on-demand agents load everything in a compact file. For those, **[SDD (Feature Specs)](.ai/templates/FEAT-TEMPLATE.md) is strongly recommended** to centralize context.

> See [supported tools & downloads](.ai/docs/supported-tools.md), [detailed tool capabilities](.ai/docs/how-claude-code-works.md) and [cross-verification patterns](.ai/docs/cross-verification.md) for more.

## Quick Start

### 1. Add `.ai/` to your project

**Option A:** Click "Use this template" on the GitHub repo (full repo clone).

**Option B:** Download only `.ai/` into an existing project (requires Node.js)

```bash
npx degit luismiglezmohino/ai-dev-agents/.ai .ai
```

**Option C:** Without Node.js

```bash
curl -sL https://github.com/luismiglezmohino/ai-dev-agents/archive/main.tar.gz \
  | tar xz && mv ai-dev-agents-main/.ai . && rm -rf ai-dev-agents-main
```

> Just [`.ai/`](.ai/) — everything else is generated by `sync.sh` in step 3.

### 2. Configure project

**Option A: Automatic bootstrap (recommended)**

Open your AI tool and paste the content of `.ai/prompts/bootstrap.md`.
The LLM analyzes your project (or asks if new) and automatically generates:
- `.ai/agents/project-context.md` — filled project context
- `CLAUDE.md` — main configuration
- `.ai/decisions.md` — initial stack decisions
- `.ai/skills/` — basic stack skills

**Option B: Manual** — copy templates and fill placeholders.

### 3. Sync and validate

```bash
.ai/sync.sh   # Generates configs for all 9 tools
.ai/test.sh   # Validates structure (0 errors = ready)
```

> Always run `sync.sh` after any change in `.ai/`. It generates `AGENTS.md`, `.claudeignore`, `docs/` structure, `.gitignore` entries, and tool-specific configs.

### What to commit vs gitignore

| File/Directory | Commit? | Why |
|---|---|---|
| `.ai/` (entire directory) | **Yes** | Source of truth |
| `CLAUDE.md` | **Yes** | Main config |
| `AGENTS.md` | **Yes** | Generated by sync.sh, but commit it |
| `.claude/settings.json` | **Yes** | Hooks + MCP config |
| `docs/` | **Yes** | Specs, ADRs (created by sync.sh) |
| `.claude/agents/`, `.claude/rules/`, `.opencode/` | **No** | Generated by `sync.sh` |
| `.cursorrules`, `.windsurfrules`, `GEMINI.md` | **No** | Generated by `sync.sh` |
| `.agents/` | **No** | Generated by `sync.sh` (Codex, Gemini CLI, Antigravity) |
| `.continue/rules/` | **No** | Generated by `sync.sh` (Continue) |
| `.gemini/skills` | **No** | Generated by `sync.sh` (Gemini CLI) |
| `.ai/.local/` | **No** | Session state |

> **Rule:** if `sync.sh` generates it, gitignore it. If you edit it by hand, commit it. Always edit in `.ai/` and re-run `sync.sh` to propagate changes. Sync generates for all 9 tools at once — this allows switching tools without reconfiguring.

## Included Agents

| Agent | Mission | Key Gates |
|---|---|---|
| product-owner | User Stories with ROI | US format, measurable criteria, clear value |
| architect | Architectural integrity | Pure Domain, e2e flow, complete contracts |
| tdd-developer | RED-GREEN-REFACTOR | Test fails first, both suites, DI compiles |
| database-engineer | Schema + migrations | Reversible, indexes, verified ORM mapping |
| security-auditor | OWASP Top 10:2025 | Validated inputs, no secrets, supply chain + conditional gates (auth, passwords, uploads, CSRF, cookies, SSRF) |
| qa-engineer | Coverage 100/80/0 | Testing pyramid, no ignored tests |
| performance-engineer | Core Web Vitals | p95 < 200ms, LCP/FID/CLS, bundle < 100KB |
| devops | CI/CD + Git workflow | Green pipeline, declarative infra, PR docs |
| observability-engineer | Metrics + logs | Business metrics, JSON logs, health checks |
| incident-responder | Incident response | Severity classified, root cause, postmortem, monitoring updated |
| technical-writer | Living docs | Functional examples, complete ADRs, setup < 15min |
| ux-designer | WCAG 2.2 AA | Contrast 4.5:1, targets 44x44px, keyboard |

Each gate exists because its absence caused real, repeated problems. See [why these gates exist](.ai/docs/why-these-gates.md).

## Architecture Support

The template supports **4 architecture modes** (~90% of all software projects):

| Flag | When to use | Agents adapted |
|---|---|---|
| `Clean` (default) | Backend with separated layers (Domain/Application/Infrastructure) | None (default gates) |
| `MVC` | Framework MVC (Laravel, Django, Rails, Spring MVC...) | architect, tdd-developer, qa-engineer, orchestrator |
| `MVVM` | Mobile apps (Android, iOS, Flutter) | architect, ux-designer, performance-engineer, orchestrator |
| `None` | Frontend-only SPA, CLI tools, or no formal architecture | Architect not invoked |

The bootstrap detects the architecture from folder structure or asks. It adapts the relevant agents — the rest stay unchanged.

> Web frontend (Vue/React) uses `None` — covered by ux-designer + skills, not by architect gates.

## Reusable Prompts

| Prompt | When to use | Required |
|---|---|---|
| `bootstrap.md` | Day 0 — generates configs + skills | Yes |
| `feature-spec.md` | Before complex features (SDD) | No |
| `code-review.md` | Before opening a PR | No |
| `refine-skills.md` | After 2-3 features | No |
| `legacy-audit.md` | Before modernizing legacy code | No |

All prompts available in English (`.ai/prompts/`) and Spanish (`.ai/prompts/es/`). Architecture-specific gates (`gates-mvc.md`, `gates-mvvm.md`) are loaded on demand by the bootstrap — no need to paste them separately.

## Working Modes

These are recommended workflows, not restrictions. The agents are flexible — use them however fits your project. All modes are compatible — use A for quick fixes and C for large features in the same project.

### Mode A: Direct agents (the simplest)

Invoke each agent manually when you need it:

```
@tdd implement the GET /api/users endpoint
@architect review the layer structure
@security audit-all
```

**Best for:** one-off tasks, quick fixes, single-layer work.

### Mode B: Orchestrator (automatic coordination)

The orchestrator routes to the correct agent and coordinates cross-verification:

```
"I need a new endpoint to manage users"
→ Routes to @tdd-developer
→ Then launches @architect and @security-auditor automatically
```

**Best for:** complete features that touch multiple layers. Available in tools with on-demand agents (Claude Code, OpenCode, Antigravity, Gemini CLI, Codex CLI).

### Mode C: Spec Driven Development (SDD) — the most complete

Define a technical specification BEFORE implementing. Agents work against that specification:

1. Generate a Feature Spec with `.ai/prompts/feature-spec.md`
2. Each agent extracts what it needs from the spec
3. Implementation guided by the specification
4. Verification against spec criteria

**Best for:** complex features (5+ endpoints), teams, critical projects. Recommended for all tools — nearly essential for Cursor/Windsurf/Copilot.

### When to use each mode

| Situation | Recommended mode |
|---|---|
| Quick fix, one-off task | A (direct agents) |
| Standard new feature | B (orchestrator) |
| Complex or critical feature | C (SDD) |
| New project (day 0) | C (SDD) for the first feature, then B |
| Production hotfix | A (direct) + @incident-responder |
| Legacy code / modernization | C (SDD) — specify before touching |

### How to work with each mode

**Mode A — Direct agents:**

1. Open your AI tool (Claude Code, OpenCode, Cursor...)
2. Type the command for the agent you need: `@tdd implement <feature>`
3. The agent works, you review what it generates
4. If you need verification, invoke manually: `@security audit-all`
5. When satisfied, commit and PR

**Mode B — Orchestrator:**

1. Describe what you need in natural language: "I need an endpoint to create users"
2. The orchestrator decides which agent to use and launches it
3. When finished, the orchestrator launches cross-verification automatically
4. You review the final result. If there are issues, the orchestrator iterates
5. When satisfied, commit and PR

**Mode C — SDD (Spec Driven Development):**

1. Before writing code, generate the specification: paste `.ai/prompts/feature-spec.md` in your AI tool
2. The AI generates a file at `docs/specs/FEAT-XXX-name.md` with requirements, contracts, expected errors
3. Review and adjust the spec — this is the time to think, not when you're coding
4. Implement: `@tdd implement according to spec docs/specs/FEAT-XXX-name.md`
5. Each agent extracts what it needs from the spec (tests, security, architecture)
6. Cross-verification against spec criteria
7. Commit and PR

## Guides

| Guide | Description |
|---|---|
| **[Quick start](.ai/docs/getting-started.md)** | From zero to working agents in 5 minutes |
| **[Cross-verification](.ai/docs/cross-verification.md)** | Orchestrator flows, when to apply, implementation strategies |
| **[Token optimization](.ai/docs/token-optimization.md)** | Language strategy, loading hierarchy, design decisions |
| **[How Claude Code works](.ai/docs/how-claude-code-works.md)** | Context management, what it can access, implications for agents |
| **[Agent lifecycle](.ai/docs/agent-lifecycle.md)** | Ephemeral agents, anti-patterns, context management |
| **[Persistent memory](.ai/docs/persistent-memory.md)** | Rules, hooks, Engram, Feature Specs, memory per tool |
| **[Why these gates exist](.ai/docs/why-these-gates.md)** | Real error chains that motivated each Quality Gate |
| **[Recommended MCPs](.ai/docs/recommended-mcps.md)** | Basic and optional MCPs by stack |
| **[Recommended hooks](.ai/docs/recommended-hooks.md)** | Git hooks with Lefthook and Husky |
| **[Recommended workflows](.ai/docs/recommended-workflows.md)** | GitHub Actions, branch strategy |
| **[Recommended models](.ai/docs/recommended-models.md)** | Model per agent/task, cost strategy, local models |

## Inspiration and References

- **[Engram](https://github.com/Gentleman-Programming/engram)** — Cross-tool persistent memory via MCP (MIT)
- **[github/spec-kit](https://github.com/github/spec-kit)** — Spec-Driven Development toolkit by GitHub (MIT)
- **[OWASP Top 10:2025](https://owasp.org/Top10/)** — Base for @security-auditor (CC BY-SA 4.0)

## License

Apache License 2.0 — Free use (including commercial) with required attribution. See [LICENSE](LICENSE).
